// STUB — replaced by the AI agent.
// Contract: <AISettingsSection /> rendered inside the Settings modal (AI tab).
export function AISettingsSection() {
  return <div style={{ color: 'var(--text-tertiary)', fontSize: 14 }}>AI settings loading…</div>;
}
