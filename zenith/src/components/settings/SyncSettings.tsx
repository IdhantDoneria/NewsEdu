// STUB — replaced by the Sync agent.
// Contract: <SyncSettingsSection /> rendered in Settings modal (Cloud sync tab).
export function SyncSettingsSection() {
  return <div style={{ color: 'var(--text-tertiary)', fontSize: 14 }}>Cloud sync settings loading…</div>;
}
