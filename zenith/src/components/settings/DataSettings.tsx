// STUB — replaced by the Sync agent.
// Contract: <DataSettingsSection /> rendered in Settings modal (Data tab):
// export / import / backup / restore, storage usage.
export function DataSettingsSection() {
  return <div style={{ color: 'var(--text-tertiary)', fontSize: 14 }}>Data tools loading…</div>;
}
