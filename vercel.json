{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd zenith && npm install && npm run build",
  "outputDirectory": "zenith/dist",
  "framework": null,
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
